apiVersion: v1
kind: Service
metadata:
  name: remediation-agent-svc
spec:
  selector:
    app: remediation-agent
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: remediation-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: remediation-agent
  template:
    metadata:
      labels:
        app: remediation-agent
    spec:
      serviceAccountName: remediation-agent-sa
      containers:
        - name: remediation-agent
          image: image-registry.openshift-image-registry.svc:5000/parashuram-n-dev/remediation-agent:latest
          ports:
            - containerPort: 8080
          env:
            - name: MY_NAMESPACE # CRITICAL FIX
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: SLACK_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: SLACK_BOT_TOKEN
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: SLACK_CHANNEL
              valueFrom:
                configMapKeyRef:
                  name: agent-config
                  key: SLACK_CHANNEL
            - name: REFLECTION_AGENT_URL
              value: "http://reflection-agent-svc:8080/log"
          livenessProbe:
            httpGet:
              path: / # Using a valid endpoint or simple health check
              port: 8080
            initialDelaySeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 10
